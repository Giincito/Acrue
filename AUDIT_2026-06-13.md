# Acrue - Auditoria de estado

Fecha: 2026-06-13  
Objetivo: estabilizar lo corregible antes de retomar desarrollo.

## Resumen ejecutivo

Actualizacion 2026-06-15: el proyecto queda en estado operativo para retomar desarrollo incremental:

- `npm test` pasa: 15 archivos, 136 tests.
- `npx tsc --noEmit` pasa.
- `npm run lint` pasa sin errores ni warnings.
- `npm run build` pasa.
- `npm audit --audit-level=moderate` pasa con 0 vulnerabilidades.
- Login ya no monta CmdK ni ChatBot sin sesion.
- Manifest e iconos PWA ya responden sin caer en `/login`.
- PWA ya registra service worker en produccion, cachea vistas criticas y muestra aviso offline.
- PWA ya incluye assets publicos dedicados 192, 512, 180 y 167px referenciados por el manifest y precacheados por el service worker.
- Las tareas recurrentes ya tienen cron diario configurado y generacion idempotente cubierta por tests.
- `/api/cron/recurrence` ya expone `GET`, que es el metodo usado por Vercel Cron, y mantiene `POST` para ejecucion manual.
- El cron de papelera usa service role para limpiar registros bajo RLS.
- La UI ya no deshabilita zoom del navegador, no permite customizar el accent global y mantiene iconos de integraciones en monocromo.
- Rutas visibles en espanol existen como canonicas; las rutas legacy en ingles redirigen a su equivalente en espanol.
- Cron jobs fallan cerrado si falta `CRON_SECRET`.
- Cron jobs ya no hacen fallback a anon key de Supabase; usan service role o fallan cerrado.
- Cron jobs ya no inicializan clientes service-role de Supabase en scope de modulo antes de validar `CRON_SECRET`.
- Cron jobs usan `createServiceClient()` como punto unico de acceso admin a Supabase.
- `createServiceClient()` valida `NEXT_PUBLIC_SUPABASE_URL` y `SUPABASE_SERVICE_ROLE_KEY` antes de crear el cliente.
- Google Calendar ya no lee `SUPABASE_SERVICE_ROLE_KEY` de forma directa; usa `createServiceClient()`.
- Google Calendar, Moodle, Gemini Vision y la descarga de fotos de Telegram pasan por `withFallback()` para degradacion centralizada.
- Google OAuth ya no guarda refresh tokens en `users.settings`; quedan en `google_integrations` con RLS sin acceso cliente.
- La pantalla de Configuracion consulta solo estado booleano de Google via API server y no expone el refresh token al renderer.
- Moodle ya tiene esquema versionado para `moodle_credentials`, `moodle_events`, RPCs de credenciales y constraint unico de eventos.
- El sync manual de Moodle ya no se llama a si mismo por HTTP ni usa `CRON_SECRET`; reutiliza el servicio server compartido y queda limitado al usuario autenticado.
- El sync cron de Moodle mantiene ejecucion global, pero los eventos se sincronizan con `upsert` idempotente para no duplicar entregas, recursos, foros ni calendario.
- Todas las RPC usadas por codigo productivo quedan protegidas por test estatico para existir en migraciones; `get_total_xp` ya esta versionada.
- Morning briefing lee `telegram_chat_id` desde la columna real de `users`, no desde `settings`.
- Cron notifications quedan protegidas contra emojis decorativos y mojibake.
- Las notificaciones Telegram productivas usan el helper compartido con `withFallback()`; las llamadas directas a Bot API quedan limitadas a rutas de prueba development-only.
- El bypass publico `x-acrue-user-id` fue eliminado de `/api/ai/router`.
- Logs que exponian estado de tokens o respuestas crudas fueron removidos.
- Undo queda alineado al PRD con ventana de 5 segundos en UI y TTL server.
- `AiThinking` queda protegido como componente de tres puntos monocromatico y se usa en esperas Gemini conocidas.
- Controles clickeables quedan cubiertos por cursor pointer desde el `Button` base y CSS global para botones nativos.
- Controles disabled quedan cubiertos por cursor `not-allowed` sin bloquear la senal visual con `pointer-events-none`.
- UI visible queda protegida contra `disabled:pointer-events-none`/`aria-disabled:pointer-events-none` y cursores disabled `default`/`auto` para no ocultar la senal de cursor disabled.
- Controles clickeables quedan cubiertos por area tactil minima de 44x44px.
- Controles nativos de formulario (`input`, `textarea`, `select`) quedan cubiertos globalmente por area tactil minima de 44x44px y `touch-action: manipulation`.
- Primitivos base de formulario/navegacion (`Input`, `Select`, `Tabs`, `Textarea`) quedan cubiertos por area tactil minima y estados disabled visibles.
- Radios sobredimensionados quedan cubiertos contra `rounded-2xl`, radios arbitrarios y CSS explicito mayores a 12px.
- Botones y links icon-only quedan protegidos con nombres accesibles y area tactil cuando corresponde; el selector de color de proyectos tambien queda etiquetado.
- Botones icon-only ya no dependen de `title` como unico nombre accesible; usan `aria-label` cuando no hay texto visible confiable.
- Botones nativos y `motion.button` con `onClick`, mas triggers `Button` de popover dentro de forms, quedan protegidos contra submits accidentales mediante `type="button"` explicito.
- Botones destructivos con icono de papelera quedan protegidos para usar tokens `destructive`, no utilidades Tailwind `red-*`, tanto en botones nativos como en el componente `Button`.
- UI visible queda protegida contra supresiones TypeScript `@ts-ignore`/`@ts-expect-error`.
- Las imagenes quedan protegidas para mantener grayscale sin overrides de color en hover.
- Dialog y Sheet base usan scrim `bg-black/40` para aislar contenido modal/drawer del fondo.
- Labels residuales de tema, chatbot y registro de comidas quedan en espanol: "Cambiar tema", "Acrue IA", "Registro de comidas" y "Dia" corregido a "Día".
- Copy visible en tareas, calendario, recordatorios y estudio queda normalizado a sentence-case con acentos en labels conocidos.
- Copy residual de Estudio/Moodle queda normalizado en acciones y toasts: "Comisión", "Evaluación", "Editar horarios", "Ver hechos" y "Ocultar hechos".
- Copy residual de evaluaciones, cronograma semanal, meal plan, chatbot y drawer de recordatorios queda normalizado: "Título", "Trabajo práctico", "Cronograma de cursada", "más tarde", "conexión" y "asistente IA".
- La UI visible deja de usar paletas Tailwind crudas `red/yellow/blue/green/emerald/amber`; estados y prioridades pasan por tokens `destructive`, `warning`, `success`, `accent` y neutros de `Design.md`.
- Copy residual de proyectos, materias, Moodle, recetas, Google Calendar, bottom nav y formularios queda normalizado con acentos y sentence-case.
- Calendario ya no usa paletas pastel/arbitrarias heredadas ni rojos de fin de semana; eventos, hoy y estados usan tokens `accent`, `success`, `warning`, `destructive` y neutros.
- La UI visible queda protegida contra tracking negativo (`tracking-tight`, tracking arbitrario negativo y `letter-spacing` negativo).
- Copy de exito visible queda mas sobrio y sin exclamaciones ruidosas en tareas, Moodle, metas de ahorro y fallback de acciones IA.
- Las rutas legacy `/brain`, `/finances`, `/habits`, `/pantry`, `/recipes`, `/settings` y `/study` redirigen a `/cerebro`, `/finanzas`, `/habitos`, `/despensa`, `/recetas`, `/configuracion` y `/estudio`.
- La lista de recordatorios ya no usa confirmacion bloqueante del navegador; la eliminacion queda en UI accesible y testeada.
- La UI visible queda protegida contra `console.*`, copy de scaffolding y `console.log` productivo.
- `console.error/warn/info/debug` directos fueron reemplazados por `logger` central con Sentry para warnings/errors y salida local para info/debug.
- Morning briefing queda programado a las 8:00 AM America/Argentina/Buenos_Aires via Vercel cron y su copy queda protegido contra emojis/exclamaciones.
- Artefactos locales de debugging (`dev.log`, `nextjs_logs.txt`, `ts_errors.txt`, `trpc_error.html`, `replace_visuals.mjs` y scripts manuales de prueba) fueron removidos del workspace y quedan protegidos por test estatico.
- Copy productiva de Telegram, crons, chatbot, recetas, CSV financiero y errores PWA queda protegida contra strings conocidos sin acentos.
- `supabase/.temp/cli-latest` fue eliminado del workspace; `supabase/.temp/` queda ignorado para que no vuelva a entrar.
- Sentry queda configurado sin DSN hardcodeado, sin PII por defecto, sin tunnel browser y con `.env*` excluido de uploads de Vercel CLI.
- Vercel Production ya tiene variables criticas cargadas y el deploy de produccion queda READY en `https://acrue.vercel.app`.
- Los cron schedules quedan compatibles con Vercel Hobby; `moodle-sync` pasa de cada 2 horas a diario para mantener presupuesto cero.

Todavia quedan pendientes funcionales/manuales antes de considerar el producto cerrado:

- PWA offline-first ya esta resuelta en codigo para app-shell, assets, nueva tarea y nuevo gasto; falta validacion manual en navegador/PWA real.
- Falta prueba real en entorno desplegado de generacion automatica de tareas recurrentes.
- Falta prueba end-to-end con bot real, webhook real y ticket real.
- Falta prueba end-to-end del morning briefing desplegado con bot real.
- Falta una pasada visual/manual completa contra `Design.md`.

## Checks finales ejecutados

| Check | Resultado |
|---|---|
| `npm test` | OK: 15 archivos, 136 tests |
| `npx tsc --noEmit` | OK |
| `npm run build` | OK |
| `npm run lint` | OK |
| `npm audit --audit-level=moderate` | OK: 0 vulnerabilidades |
| `git diff --check` | OK: solo warnings LF/CRLF |
| HTTP local `/login` | OK: 200 |
| HTTP local `/manifest.webmanifest` | OK: 200, `application/manifest+json`, JSON valido |
| HTTP local `/icon` y `/apple-icon` | OK: 200, `image/png` |
| HTTP aliases protegidos | OK: `/finanzas`, `/despensa`, `/recetas`, `/habitos`, `/cerebro`, `/configuracion`, `/hoy`, `/semanal`, `/foco` devuelven 307 a login sin sesion |

Nota: el Browser plugin no pudo acceder a `127.0.0.1:3000`/`localhost:3000` desde el navegador embebido y mostro `ERR_CONNECTION_REFUSED`, aunque el servidor respondia correctamente por `Invoke-WebRequest`. La verificacion local se hizo por HTTP directo.

## Corregido en esta pasada

### 1. Build de produccion

Estado: resuelto.

Cambios:
- `src/app/(app)/layout.tsx`: `Sidebar` envuelto en `Suspense`.
- `src/app/(app)/calendario/page.tsx`: `CalendarView` envuelto en `Suspense`.

Resultado:
- `npm run build` ya pasa.

### 2. UI autenticada montada en login

Estado: resuelto.

Cambios:
- `src/app/layout.tsx`: root layout queda solo con providers base y toaster.
- `src/app/(app)/layout.tsx`: `CmdK` y `ChatBotFab` se montan solo dentro del shell autenticado.

Resultado:
- `/login` no incluye CmdK ni ChatBot en HTML local.

### 3. PWA manifest e iconos

Estado: resuelto para los flujos abordados.

Cambios:
- `src/proxy.ts`: excluye `manifest.webmanifest`, `icon`, `apple-icon`, service worker y assets PWA.
- `src/app/manifest.ts`: apunta a PNG publicos dedicados en `/icons`, con `theme_color` y `background_color` `#0C0C0B`.
- `src/app/icon.tsx` y `src/app/apple-icon.tsx`: reemplazan la "A" azul por el isotipo orbital monocromatico de `Design.md`.
- `src/app/layout.tsx`: `lang="es"`, `themeColor="#0C0C0B"`, `appleWebApp.statusBarStyle="black-translucent"`.
- `public/icons/icon-192.png`, `icon-512.png`, `icon-180.png` e `icon-167.png`: assets dedicados con isotipo Acrue.
- `public/sw.js` y `src/lib/pwa/offline.ts`: precachean los PNG publicos.
- `README.md` y `Roadmap.md`: documentan la alternativa equivalente a `next-pwa` basada en service worker propio.

Pendiente:
- Falta probar instalacion y navegacion offline en PWA real.

### 3.b Tareas recurrentes

Estado: resuelto en codigo; pendiente de validacion real desplegada.

Cambios:
- `vercel.json`: agrega `/api/cron/recurrence` a las 03:00 UTC, equivalente a 00:00 America/Argentina/Buenos_Aires.
- `src/lib/utils/recurrence.ts`: agrega generacion de instancias recurrentes con preservacion de hora original e idempotencia por `metadata`.
- `src/app/api/cron/recurrence/route.ts`: usa el helper compartido, falla cerrado si falta configuracion server, evita duplicados por dia y expone `GET` para Vercel Cron.

Tests:
- `src/lib/utils/__tests__/recurrence.test.ts`.
- `src/lib/project-hygiene.test.ts` protege que el cron quede configurado.
- `src/lib/project-hygiene.test.ts` protege que toda ruta en `vercel.json` tenga handler `GET`.

Pendiente:
- Probar una tarea recurrente real en entorno desplegado con Supabase y Vercel Cron.

### 4. Seguridad de IA y cron

Estado: resuelto para los hallazgos abordables.

Cambios:
- `src/app/api/ai/router/route.ts`: elimina `x-acrue-user-id`.
- `src/lib/server/cron-auth.ts`: nuevo helper `assertCronRequest()`.
- Cron routes actualizadas para usar `assertCronRequest()`.
- `src/app/api/cron/cleanup-trash/route.ts`: usa service role en lugar del cliente con cookies de usuario.
- `src/app/api/cron/morning-briefing/route.ts` y `src/app/api/cron/google-sync/route.ts`: usan `createServiceClient()` sin fallback a anon key.
- `src/app/api/cron/enrollment-alerts/route.ts` y `src/app/api/cron/moodle-sync/route.ts`: mueven `createServiceClient()` dentro del handler, despues de `assertCronRequest()`.
- `src/app/api/cron/debt-reminders/route.ts`, `src/app/api/cron/recurrence/route.ts` y `src/app/api/cron/telegram-briefing/route.ts`: dejan de importar `@supabase/supabase-js` y usan `createServiceClient()`.
- `src/utils/supabase/service.ts`: valida env vars de service role y falla cerrado antes de llamar a Supabase.
- `src/lib/google-calendar.ts`: usa `createServiceClient()` en lugar de crear clientes service-role directos.
- `src/lib/google-calendar.ts`: encapsula operaciones externas de Google Calendar con `withFallback()`.
- `src/lib/moodle/client.ts`: `withMoodleFallback()` y la prueba de conexion delegan en `withFallback()`.
- `src/lib/gemini/vision.ts`: el analisis de tickets por Gemini Vision pasa por `withFallback()` y degrada a `null` si falla la API o el JSON.
- `src/lib/telegram.ts`: la descarga de fotos para Vision usa `withFallback()` con timeout explicito.
- `src/lib/telegram.ts`: agrega `sendTelegramMessage()` para centralizar envios productivos con `withFallback()`.
- `supabase/migrations/20260615000000_add_google_integrations.sql`: agrega tabla server-only `google_integrations`, migra tokens existentes desde `users.settings` y elimina esa clave del JSON.
- `src/app/api/auth/google/callback/route.ts`, `src/app/api/cron/google-sync/route.ts`, `src/app/api/integrations/status/route.ts` y `src/lib/google-calendar.ts`: usan `google_integrations` para tokens de Google.
- `src/app/api/cron/morning-briefing/route.ts`: usa la columna `users.telegram_chat_id`, consistente con Telegram y Configuracion.
- `src/app/api/cron/debt-reminders/route.ts`: elimina el join no usado `auth_users:user_id(id)` que podia romper la consulta de PostgREST.
- Crons productivos y `src/lib/despensa/telegramHelper.ts` envian Telegram mediante `sendTelegramMessage()` para degradar por `withFallback()` sin construir URLs `botundefined`.
- `src/app/api/integrations/google-sync/route.ts`: usa `createServiceClient()` para upsert con service role, no `createServerClient()` con cookies vacias.
- `MOODLE_ENCRYPTION_KEY` ya no cae a `CRON_SECRET` ni a `default-key`; falla si falta.
- `src/server/routers/moodle.ts`: ya no usa `dev-secret`.
- `src/lib/moodle/sync.ts`: centraliza el sync de Moodle para cron y ejecucion manual.
- `src/server/routers/moodle.ts`: el sync manual queda scopiado a `ctx.user.id` y deja de hacer self-call HTTP a `/api/cron/moodle-sync`.
- `src/app/api/cron/moodle-sync/route.ts`: queda como wrapper autenticado por `CRON_SECRET` sobre `syncMoodleUsers()`.
- `src/lib/moodle/sync.ts`: usa `upsert` idempotente sobre `moodle_events` y evita crear tareas Moodle repetidas cuando el evento ya existia.
- `supabase/migrations/20260615010000_add_moodle_integration_schema.sql`: versiona `moodle_credentials`, `moodle_events`, RPCs `store_moodle_creds`, `decrypt_moodle_creds`, `delete_moodle_creds`, `get_total_xp` e indices necesarios.
- `src/app/api/integrations/moodle/route.ts`: usa service client despues de autenticar usuario para leer/actualizar credenciales Moodle sin exponer tokens al cliente.
- Rutas de test de Telegram desactivadas fuera de development y sin chat hardcodeado.
- Logs de `GEMINI_API_KEY`, `TELEGRAM_BOT_TOKEN` y respuestas crudas de Gemini removidos.
- Copy de crons de Telegram limpiado para respetar tono sobrio, sin emojis decorativos ni texto corrupto.

Tests agregados:
- `src/lib/server/cron-auth.test.ts`.
- `src/lib/gemini/actions.test.ts`.
- `src/lib/project-hygiene.test.ts` protege service-role en crons, inicializacion dentro del request, `telegram_chat_id` dedicado, copy de cron sin mojibake/emoji, Telegram centralizado en helper con fallback y uso correcto del helper service Supabase.
- `src/utils/supabase/service.test.ts` protege que el helper service falle cerrado sin env vars y use la service-role key cuando esta configurada.
- `src/lib/project-hygiene.test.ts` protege que la service-role key cruda quede limitada al helper y que clientes externos server usen `withFallback()`.
- `src/lib/project-hygiene.test.ts` protege que los `sendMessage` productivos de Telegram pasen por el helper con fallback.
- `src/lib/project-hygiene.test.ts` protege que `google_refresh_token` no aparezca en componentes cliente ni en `users.settings`.
- `src/lib/project-hygiene.test.ts` protege que routers protegidos no hagan self-call a cron ni dependan de `CRON_SECRET`.
- `src/lib/project-hygiene.test.ts` protege que el sync manual de Moodle quede scopiado al usuario autenticado.
- `src/lib/project-hygiene.test.ts` protege que Moodle tenga esquema versionado, eventos idempotentes y que toda RPC productiva exista en migraciones.
- `src/lib/moodle/client.test.ts` protege que Moodle degrade mediante el helper compartido.
- `src/lib/google-calendar.test.ts` mockea `createServiceClient()` para que los tests no acepten acceso service-role directo.

### 5. Contratos IA -> DB

Estado: resuelto.

Cambios:
- `create_habit`: usa columna `active`, no `is_active`.
- `create_project`: usa `due_at`, no `due_date`.
- `add_wishlist_item`: usa `status: "wanted"`, no `"deseado"`.
- `create_note`: deja de mapear a tabla `notes`, porque no existe migracion.
- `create_event`: escribe en `calendar_events` con `start_at`, `end_at` y `source: "ai"`, no en `reminders`.
- `calendar_events`: nueva migracion agrega `deleted_at` para undo y un indice unico por `user_id, gcal_event_id`.
- `integrations.googleCalendarEvents`: filtra eventos con `deleted_at`, usa el `id` local cuando no hay `gcal_event_id` y distingue eventos locales de Google.
- Google sync manual y cron: usan columnas reales de `calendar_events` y scopean el upsert/busqueda por usuario.
- Contexto de Gemini: deja de consultar la columna inexistente `location` en `calendar_events`.
- `executeAiAction`: valida con Zod todos los intents antes de construir rows o llamar a Supabase.
- `src/lib/gemini/actions.ts`: payload tipado con `unknown` + helpers de lectura.

### 6. Rutas visibles

Estado: resuelto para los flujos abordados.

Aliases/wrappers agregados:
- `/finanzas`
- `/despensa`
- `/recetas`
- `/habitos`
- `/cerebro`
- `/configuracion`
- `/hoy`
- `/semanal`
- `/foco`

Cambios:
- `Sidebar`, `BottomNav` y `CmdK` apuntan a rutas en espanol.
- Las carpetas inglesas (`finances`, `pantry`, `recipes`, `habits`, `brain`, `settings`, `study`) siguen existiendo solo como compatibilidad legacy y redirigen a rutas canonicas en espanol.

Pendiente:
- Si se quiere limpieza fisica total, migrar/eliminar carpetas legacy en una pasada dedicada cuando no haga falta compatibilidad de URLs.
- `/foco` es solo placeholder; el modulo real sigue pendiente de Roadmap Fase 7.

### 7. Repo/env

Estado: resuelto para los hallazgos abordables sin operar el index de Git.

Cambios:
- `.gitignore`: permite trackear `.env.example` con `!.env.example`.
- `.gitignore`: ignora `supabase/.temp/`.
- `.env.example`: agrega `TELEGRAM_TEST_CHAT_ID=`.
- `.gitignore`: ignora artefactos locales de debugging y scripts manuales obsoletos.
- Se eliminaron del workspace `dev.log`, `nextjs_logs.txt`, `ts_errors.txt`, `trpc_error.html`, `replace_visuals.mjs`, `scripts/test-db-error.ts`, `scripts/test-moodle-calendar.ts` y `supabase/.temp/cli-latest`.

Nota:
- Al commitear estas eliminaciones, `supabase/.temp/cli-latest` deja de estar versionado.

### 8. Bug funcional en suscripciones

Estado: resuelto.

Cambio:
- `src/components/finances/SubscriptionForm.tsx`: elimina acceso a `resetForm` antes de declaracion y evita sincronizar state dentro de `useEffect`; el form se inicializa por remount.

### 9. Lint global

Estado: resuelto.

Cambios principales:
- Componentes de estudio, finanzas, despensa y recetas tipados sin `any` explicito.
- `src/lib/gemini/chat.ts` y `src/lib/gemini/study-planner.ts` tipados para relaciones de Supabase.
- `src/utils/supabase/middleware.ts` deja de declarar `options` donde no se usa.
- Excepcion documentada para `<img>` en recetas porque `image_url` acepta URLs libres del usuario y `next/image` requeriria una allowlist de hosts que romperia datos validos.

Resultado:
- `npm run lint` pasa sin errores ni warnings.

### 10. Dependencias y vulnerabilidades

Estado: resuelto para `npm audit`.

Cambios:
- `next`: `16.1.6` -> `16.2.9`.
- `eslint-config-next`: `16.1.6` -> `16.2.9`.
- `cmdk`: `0.2.1` -> `1.1.1` para compatibilidad con React 19.
- `overrides`: `micromatch -> picomatch@2.3.2`.
- `overrides`: `postcss@8.5.15` global para eliminar el `postcss@8.4.31` transitivo de Next.

Resultado:
- `npm audit --audit-level=moderate` reporta 0 vulnerabilidades.

### 11. Build reproducible y warnings de framework

Estado: resuelto.

Cambios:
- `src/app/layout.tsx`: elimina `next/font/google` para que `next build` no dependa de descargar Google Fonts.
- `src/app/globals.css`: define `--font-geist-sans` y `--font-geist-mono` con pilas de sistema.
- `next.config.ts`: migra opciones deprecated de Sentry a `webpack.reactComponentAnnotation` y `webpack.treeshake.removeDebugLogging`.
- `src/middleware.ts` migrado a `src/proxy.ts` con export `proxy`, como espera Next 16.
- `src/app/icon.tsx` y `src/app/apple-icon.tsx`: eliminan `runtime = 'edge'` para que Next los genere como rutas estaticas.

Resultado:
- `npm run build` pasa limpio con Next `16.2.9`.

### 12. Bugs funcionales detectados durante lint

Estado: resuelto para los casos encontrados.

Cambios:
- `src/lib/gemini/study-planner.ts`: `getUpcomingExams` filtra por `user_id`; antes podia leer assignments fuera del scope del usuario.
- `src/server/routers/mealLog.ts`: `getDaySummary` devuelve `id`, necesario para que el cliente pueda eliminar comidas logueadas.
- `src/lib/gemini/chat.ts`: el prompt deja de pedir emojis y el fallback visible de IA ya no usa simbolos decorativos.

### 13. Telegram sin side effects en development

Estado: resuelto en codigo.

Cambios:
- `src/lib/telegram.ts`: importar el modulo ya no arranca polling ni muta webhooks.
- `src/lib/telegram.ts`: agrega `configureTelegramRuntime()` para configurar webhook en runtime y polling local solo con `TELEGRAM_ENABLE_DEV_POLLING=true`.
- `src/instrumentation.ts`: usa `configureTelegramRuntime()` y elimina el log decorativo.
- `.env.example`: agrega `TELEGRAM_ENABLE_DEV_POLLING`, `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_APP_URL` y `MOODLE_URL`.

Tests:
- `src/lib/telegram.test.ts`.

### 14. Roadmap y README

Estado: resuelto para documentacion operativa.

Cambios:
- `README.md`: reemplaza el template de Next por guia real de Acrue, setup, env vars, comandos, Supabase, Telegram y estado actual.
- `Roadmap.md`: agrega bloque de estado auditado y corrige checks de PWA/iPhone/Telegram E2E que no estaban verificados.

### 15. Pasada visual automatizada contra Design.md

Estado: parcialmente resuelto.

Cambios:
- `src/components/layout/bottom-nav.tsx`: implementa bottom nav mobile con boton central de Cmd+K.
- `src/components/ui/cmdk.tsx`: escucha el evento global `acrue:open-cmdk`.
- Se eliminaron `font-semibold`, `font-bold`, `font-black`, `font-extrabold` y `font-weight >= 600` en UI web.
- Se reemplazo `rounded-2xl` y `rounded-[24px]` por radios menores en UI web.
- Se eliminaron emojis visibles en UI web detectados en componentes.
- `src/app/layout.tsx`: ya no deshabilita zoom con `maximumScale`/`userScalable`.
- `src/components/providers/theme-provider.tsx`, `src/app/(app)/settings/page.tsx` y `src/app/globals.css`: se elimina customizacion de accent global para respetar `#2282fa` fijo.
- `src/components/ui/undo-toast.tsx`: usa `var(--accent)` en lugar de `hsl(var(--accent))`.
- `src/components/ui/undo-toast.tsx` y `src/lib/gemini/actions.ts`: ventana de Undo alineada al PRD en 5 segundos.
- `src/components/ui/button.tsx` y `src/app/globals.css`: cursor pointer explicito para `Button` base, botones nativos y roles de boton.
- `src/components/ui/button.tsx` y `src/app/globals.css`: cursor `not-allowed` explicito en botones disabled sin `pointer-events-none` en el `Button` base.
- `src/app/(app)/tareas/proyectos/page.tsx`: elimina `disabled:pointer-events-none` del trigger de nuevo proyecto para preservar cursor disabled.
- `src/components/ui/button.tsx` y `src/app/globals.css`: area tactil minima de 44x44px y `touch-action: manipulation` para controles clickeables.
- `src/app/globals.css`: `input`, `textarea` y `select` nativos reciben area tactil minima de 44x44px.
- `src/components/ui/input.tsx`, `src/components/ui/select.tsx` y `src/components/ui/tabs.tsx`: area tactil minima y estados disabled sin ocultar el cursor `not-allowed`.
- `src/components/tasks/task-list-view.tsx`: elimina radio arbitrario `24px` para respetar la escala compacta de `Design.md`.
- Botones icon-only de Tareas, Estudio, Calendario, Moodle, Despensa, Recetas, Sidebar, Recordatorios, Finanzas y selector de color de Proyectos reciben `aria-label`/nombre accesible.
- Controles compactos con texto dinamico o tooltip en Calendario, Estudio, Despensa, Proyectos, Recetas, Recordatorios, Tareas e IconPicker reciben `aria-label` explicito.
- Botones de eliminar en Estudio y Tareas usan el token `destructive` en lugar de `red-*`.
- Botones nativos con handlers de click en Calendario, Finanzas, Despensa, Proyectos, Recordatorios, Tareas, CmdK e IconPicker reciben `type="button"` explicito para evitar submits accidentales.
- Links icon-only de Moodle reciben nombre accesible y area tactil minima.
- `src/components/reminders/reminder-list-view.tsx`: reemplaza `confirm()` por confirmacion propia, no bloqueante, con controles accesibles.
- `src/components/recipes/RecipeCard.tsx`: mantiene las imagenes en escala de grises tambien en hover.
- `src/app/(app)/settings/page.tsx`: iconos de integraciones quedan monocromaticos y el link externo de Telegram usa `noopener,noreferrer`.

Tests:
- `src/components/layout/bottom-nav.test.tsx`.
- `src/lib/design-system.test.ts`.

## Pendientes que quedan

### A. Lint global

Estado: resuelto.

Resultado:
- `npm run lint` pasa sin errores ni warnings.

### B. Vulnerabilidades npm

Estado: resuelto.

Resultado:
- `npm audit --audit-level=moderate` reporta 0 vulnerabilidades.
- Verificado despues de actualizar dependencias y overrides.

### C. Telegram vision y escaneo de tickets

Estado: resuelto en codigo.

Cambios:
- `src/lib/telegram.ts` procesa tickets con `analyzeReceipt` y registra gastos via `executeAiAction`.
- Se eliminaron inserciones directas a columnas inexistentes (`category`, `payment_method`).
- `src/app/api/telegram/route.ts` deja de duplicar logica y delega el webhook a los handlers de Telegraf.

Pendiente:
- Prueba end-to-end con bot real, webhook real y una foto de ticket real.

### D. Chatbot y Undo

Estado: parcialmente resuelto.

Cambios:
- `src/app/api/ai/chat/route.ts` usa `executeAiAction` en vez de insertar acciones manualmente.
- La ruta devuelve `actionId`, `undoId`, `actionMessage` y `actionSuccess`.
- `src/components/ui/chatbot-fab.tsx` usa `undoId` real para mostrar deshacer.
- Telegram ya no persiste historial en `ai_chat_messages`; usa contexto stateless como indica el PRD v1.

### E. PWA offline

Estado: resuelto en codigo; pendiente de validacion manual real.

Problema:
- La app tiene manifest, iconos, service worker, cache app-shell y aviso offline.
- El manifest usa PNG publicos dedicados y el service worker los precachea.
- Nueva tarea y nuevo gasto se encolan en IndexedDB cuando no hay conexion.
- Al recuperar conexion, `OfflineSyncStatus` sincroniza contra `/api/pwa/offline-sync` usando los routers protegidos existentes.
- Las acciones sincronizadas se eliminan de IndexedDB y las fallidas quedan con contador de reintentos y ultimo error.

Accion recomendada:
- Probar la PWA instalada en iPhone y navegacion offline real.
- Revisar si se quiere ampliar offline queue a otras mutaciones ademas de nueva tarea y nuevo gasto.

### F. Roadmap y README

Estado: resuelto para documentacion operativa.

Resultado:
- `README.md` ya documenta setup, env vars, comandos, Supabase, Telegram y estado actual.
- `Roadmap.md` ya incluye un bloque de estado auditado y corrige checks claramente no verificados.

Accion recomendada:
- Usar este audit como fuente de verdad temporal.
- Seguir actualizando el Roadmap por modulo despues de verificar funcionalmente cada uno.

### G. UI/Design.md

Estado: parcialmente resuelto.

Resuelto:
- Test estatico protege que UI web no use font weights > 500.
- Test estatico protege que UI web no use `rounded-2xl` ni radios arbitrarios mayores a 12px.
- Test estatico protege que CSS explicito en UI web no use `border-radius` mayor a 12px, excepto pills.
- Test estatico protege que UI web no use emojis visibles.
- Test estatico protege que UI web no use mojibake visible ni labels ingleses conocidos.
- Test estatico protege labels conocidos con title-case inconsistente o acentos faltantes en UI visible.
- Test estatico protege que los controles base de cierre de Dialog/Sheet usen copy en espanol.
- Test estatico protege que los scrims base de Dialog/Sheet no queden demasiado debiles.
- Test estatico protege que `AiThinking` sea monocromatico, sin icono decorativo, y que aparezca en esperas Gemini conocidas.
- Test estatico protege que controles clickeables mantengan area tactil minima de 44x44px.
- Test estatico protege que controles nativos de formulario mantengan area tactil minima global.
- Test estatico protege que primitivos base de formulario/navegacion mantengan area tactil minima y estados disabled visibles.
- Test estatico protege que UI visible no use `disabled:pointer-events-none` ni `aria-disabled:pointer-events-none`.
- Test estatico protege que UI visible no use `disabled:cursor-default`/`disabled:cursor-auto` ni sus variantes `aria-disabled`.
- Test estatico protege que botones y links icon-only tengan nombre accesible y que el selector de color de Proyectos sea navegable por asistencia.
- Test estatico protege que botones icon-only nativos no dependan solo de `title`.
- Test estatico protege que botones nativos y componentes `Button` con icono `Trash2` no usen utilidades `red-*`.
- Test estatico protege que botones nativos y `motion.button` con `onClick` declaren `type` explicito y que triggers `Button` de popover dentro de forms usen `type="button"`.
- Test estatico protege que UI visible no use supresiones TypeScript.
- Test estatico protege que las imagenes no remuevan grayscale con overrides de hover.
- Test estatico protege que la UI visible no use confirmaciones bloqueantes del navegador.
- Test estatico protege que UI visible no escriba en `console.*` y que no haya `console.log` en codigo productivo.
- Test estatico protege que todo diagnostico productivo pase por `src/lib/server/logger.ts`.
- Test estatico protege que el morning briefing este programado a las 8:00 AM de Buenos Aires y que su copy respete el tono de `Design.md`.
- Test estatico protege que artefactos locales de debugging no queden en el workspace del repo.
- Test estatico protege copy conocida sin acentos en Telegram, crons, chatbot, recetas, CSV financiero y errores PWA.
- Test estatico protege que Sentry no tenga DSN hardcodeado, PII por defecto ni tunnel browser.
- Test estatico protege que Vercel no suba `.env*` desde CLI y que los crons sean compatibles con Hobby.
- Test estatico protege que UI visible no use tracking negativo y que rutas legacy en ingles redirijan a rutas canonicas en espanol.
- Bottom nav mobile ya incluye boton central de Cmd+K.
- Bottom nav usa "Inicio" en lugar de "Dashboard"; rutas legacy en ingles redirigen a rutas canonicas en espanol.
- `/wishlist` ya no muestra copy de scaffolding; usa "Lista de deseos" como vista vacia minima.
- Ajustes legacy usa labels explicitos en espanol para modulos configurables y elimina copy mixto tipo "Telegram Bot", "Chat ID" y titulos en ingles.
- Dialog y Sheet base anuncian sus controles de cierre como "Cerrar", no "Close".
- Dialog y Sheet base usan scrim `bg-black/40` para separar el primer plano del fondo.
- Tema, chatbot y registro de comidas ya no exponen copy residual en ingles tipo "Toggle theme", "Acrue AI" o "Meal Tracker".
- Tareas, calendario, recordatorios y estudio corrigen labels conocidos como "Nueva tarea", "Fecha inicio", "Actividad reciente", "Nueva evaluación", "Código" y "Comisión".
- Estudio/Moodle corrige acciones y toasts residuales sin acentos o title-case: "Evaluación agregada", "Eliminar evaluación", "Editar horarios", "Ver hechos" y "Ocultar hechos".
- Evaluaciones, cronograma semanal, meal plan, chatbot y drawer de recordatorios corrigen labels residuales sin acentos, title-case o copy mixto: "Título", "Trabajo práctico", "Cronograma de cursada", "más tarde", "conexión", "asistente IA" y "Recordatorio activo".
- Test estatico protege que la UI visible no use paletas cromaticas Tailwind crudas ni paletas legacy de proyectos/tareas/recordatorios fuera de tokens semanticos de `Design.md`.
- Proyectos, materias, Moodle, recetas, Google Calendar, bottom nav y formularios corrigen copy residual: "Crear proyecto", "Detalles del proyecto", "Notas del evento", "Añadir notas", "Materia agregada", "Todavía no tenés", "Más" y "Proyecto (opcional)".
- Calendario corrige paletas heredadas y copy residual: "Crear evento"; el dia actual usa `accent` y los eventos usan tokens semanticos.
- Copy ruidosa residual queda normalizada: "Bandeja limpia.", "Conectado a Moodle UNICEN.", "Meta alcanzada. +50 XP" y "Dato registrado.".
- Superficies residuales raw como `#ECECEC` en lista de tareas pasan a tokens (`secondary`).
- Copy residual sin acentos queda normalizada en Telegram, morning briefing, alertas financieras, chatbot, recetas, meal plan, CSV financiero y errores offline.

Pendiente:
- Puede quedar copy mixto ingles/espanol en modulos no cubiertos por la lista estatica de labels conocidos.
- Falta una pasada visual/manual completa en navegador contra `Design.md`.

Accion recomendada:
- Hacer una pasada visual dedicada en navegador y corregir copy/layout por modulo.

### H. Warnings de framework

Estado: resuelto para build.

Cambios:
- Sentry usa `webpack.reactComponentAnnotation` y `webpack.treeshake.removeDebugLogging`.
- Next usa `src/proxy.ts` en lugar de `src/middleware.ts`.
- Iconos PWA ya no fuerzan Edge runtime.

Pendiente menor:
- Dev server: el polling local de Telegram quedo opt-in con `TELEGRAM_ENABLE_DEV_POLLING=true`.

### I. Dev server local desde Codex

Estado: parcialmente resuelto, con bloqueo visual.

Observacion:
- `npm run dev -- --hostname 127.0.0.1` queda vivo, pero la herramienta corta por timeout si se ejecuta en primer plano.
- Dejarlo en segundo plano con `Start-Process` requiere sanitizar variables duplicadas `Path/PATH` en Windows.
- Con esa sanitizacion, `/login` respondio 200 por HTTP directo en `127.0.0.1:3000`.
- El Browser plugin no pudo inicializar por sandbox de Windows: `CreateProcessAsUserW failed: 5`.
- La interferencia de instrumentation/Telegram en development fue corregida; falta prueba visual manual o Browser plugin funcional.

Accion recomendada:
- Levantarlo manualmente en una terminal normal con `npm run dev -- --hostname 127.0.0.1`.
- Probar visualmente `http://127.0.0.1:3000/login` y la PWA instalada cuando el navegador automatizado este disponible.

## Estado actual recomendado

Se puede retomar trabajo de producto con mas seguridad porque `lint`, `typecheck`, `test`, `build` y `audit` pasan. Antes de abrir features grandes conviene cerrar, como minimo:

1. Prueba end-to-end de Telegram/chat/vision con bot real y webhook real.
2. Validacion manual de PWA offline instalada en iPhone/navegador real.
3. Prueba real de tareas recurrentes en entorno desplegado.
4. Pasada visual/manual contra `Design.md`.
